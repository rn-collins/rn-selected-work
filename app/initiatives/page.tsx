import type { Metadata } from "next"; import Link from "next/link"; import { CanonicalPage } from "../../components/canonical-page"; import { siteProperties } from "../../lib/site-properties";
export const metadata:Metadata={title:"Initiatives",description:"Named bodies of work held within RN's practice."};
export default function Page(){const initiatives=siteProperties.filter((property)=>property.kind==="initiative"&&property.public);return <CanonicalPage><header className="canonicalSubhero shell"><p className="eyebrow">Initiatives</p><h1>Long-running questions.<br/><em>Work that keeps growing.</em></h1><p>Some questions are larger than one client engagement or one build. These initiatives give RN a place to keep researching, testing, publishing, and building around them.</p></header><section className="canonicalSection shell"><div className="canonicalSectionHead"><p className="eyebrow">Current initiatives</p><p>Each page states what exists now—and what is still being developed.</p></div><div className="canonicalCards">{initiatives.map((initiative)=><article key={initiative.id}><p className="eyebrow">{initiative.lifecycle}</p><h2>{initiative.name}</h2><p>{initiative.purpose}</p>{initiative.evidenceBoundary&&<small>{initiative.evidenceBoundary}</small>}{initiative.destination&&<Link href={initiative.destination}>See the work and current status →</Link>}</article>)}</div></section></CanonicalPage>}
