import type { Metadata } from "next"; import Link from "next/link"; import { CanonicalPage } from "../../components/canonical-page"; import { projects } from "../../lib/portfolio"; import { builds, populatedCategories } from "../../lib/builds";
export const metadata:Metadata={
  alternates: { canonical: "/work" },title:"Selected Work",description:"Selected research, systems, frameworks, public resources, and working builds by RN Collins."};
export default function Page(){return <CanonicalPage><header className="canonicalSubhero shell"><p className="eyebrow">Selected Work</p><h1>See the problem.<br/><em>See what I made.</em></h1><p>These case studies show what RN noticed, how she researched it, what she built, who it was for, and what the available evidence can—and cannot—prove.</p><div className="canonicalActions"><Link className="button" href="#primary-cases">Start with the case studies →</Link><Link className="button secondary" href="#build-atlas">Browse every public build →</Link></div></header><section id="primary-cases" className="canonicalSection shell"><div className="canonicalSectionHead"><div><p className="eyebrow">Primary case studies</p><h2>Read the reasoning,<br/>not just the result.</h2></div><p>{projects.length} substantial project records.</p></div><div className="canonicalCards">{projects.map((project)=><article key={project.slug}><p className="eyebrow">{project.kind} · {project.status}</p><h2>{project.shortTitle}</h2><p>{project.thesis}</p><Link href={`/work/${project.slug}`}>Read what RN saw and built →</Link></article>)}</div></section><section id="build-atlas" className="canonicalBand"><div className="shell"><div className="canonicalSectionHead"><div><p className="eyebrow">All public builds</p><h2>Tools, systems, resources,<br/>courses, and experiments.</h2></div><p>{builds.length} builds, generated from live data.</p></div><p className="rosterNote">This list is not maintained by hand. A script asks GitHub what repositories exist and Vercel what is deployed, checks that each production URL answers, and writes the data these pages render from. Each build carries the problem it addresses, what it does, what it runs on, and an honest status — retired prototypes are listed as retired, paused work as paused, and the index says what it deliberately leaves out.</p><div className="workCollectionLinks workCategoryLinks">{populatedCategories.map((category)=><Link href={`/builds#${category.id}`} key={category.id}>{category.title}<span>{category.builds.length} builds</span></Link>)}</div><div className="canonicalActions"><Link className="button" href="/builds">Open the complete build index →</Link></div></div></section></CanonicalPage>}
